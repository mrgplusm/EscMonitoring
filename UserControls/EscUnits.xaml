<UserControl x:Class="Monitoring.UserControls.EscUnits"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <Style TargetType="Canvas" x:Key="UnitStyle" >
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="10" />
                </Setter.Value>
            </Setter>
        </Style>
        <Canvas x:Key="cnvUnit" Width="110" Height="85" Background="Gray" Grid.Column="5" Grid.RowSpan="2" Grid.Row="2"  Style="{StaticResource UnitStyle}" >
            <TextBlock Text="ESC Slave" Foreground="White" FontWeight="Bold" FontSize="20" />
        </Canvas>
    </UserControl.Resources>
    <Grid>
        <Canvas x:Name="myCanvas">
            
        </Canvas>        
    </Grid>
</UserControl>
